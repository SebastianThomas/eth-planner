apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Not applied directly - prod/ overlays this with its namespace.
# namespace.yaml's name is a placeholder the overlay's `namespace:` rewrites.
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
